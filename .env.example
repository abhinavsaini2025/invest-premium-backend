# Comma-separated browser origins allowed to call the API.
# Keep localhost for local Vite development and add the deployed frontend origin.
CORS_ORIGINS=http://localhost:5173,https://hexagrow.netlify.app
